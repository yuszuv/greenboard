de:
  validation:
    or: "oder"

    errors:
      unexpected_key: "ist nicht zulässig"

      array?: "muss ein Array sein"

      empty?: "muss leer sein"

      excludes?: "darf nicht %{value} enthalten"

      excluded_from?:
        arg:
          default: "must not be one of: %{list}"
          range: "must not be one of: %{list_left} - %{list_right}"

      exclusion?: "must not be one of: %{list}"

      eql?: "must be equal to %{left}"

      not_eql?: "must not be equal to %{left}"

      filled?: "muss ausgefüllt werden"

      format?: "hat kein gültiges Format"

      number?: "muss eine Nummer sein"

      odd?: "muss ungerade sein"

      even?: "muss gerade sein"

      gt?: "muss größer sein als %{num}"

      gteq?: "muss größer oder gleich %{num} sein"

      hash?: "muss ein Hash sein"

      included_in?:
        arg:
          default: "muss enthalten sein in: %{list}"
          range: "muss eins sein von: %{list_left} - %{list_right}"

      inclusion?: "muss enthalten sein in: %{list}"

      includes?: "muss %{value} enthalten"

      bool?: "must be boolean"

      true?: "must be true"

      false?: "must be false"

      int?: "must be an integer"

      float?: "must be a float"

      decimal?: "must be a decimal"

      date?: "must be a date"

      date_time?: "must be a date time"

      time?: "must be a time"

      key?: "is missing"

      attr?: "is missing"

      lt?: "must be less than %{num}"

      lteq?: "must be less than or equal to %{num}"

      max_size?: "size cannot be greater than %{num}"

      max_bytesize?: "bytesize cannot be greater than %{num}"

      min_size?: "size cannot be less than %{num}"

      min_bytesize?: "bytesize cannot be less than %{num}"

      nil?: "cannot be defined"

      str?: "must be a string"

      type?: "must be %{type}"

      respond_to?: "must respond to %{method}"

      size?:
        arg:
          default: "size must be %{size}"
          range: "size must be within %{size_left} - %{size_right}"

        value:
          string:
            arg:
              default: "length must be %{size}"
              range: "length must be within %{size_left} - %{size_right}"

      bytesize?:
        arg:
          default: "must be %{size} bytes long"
          range: "must be within %{size_left} - %{size_right} bytes long"

      uuid_v4?: "is not a valid UUID"

      not:
        empty?: "cannot be empty"
    rules:
      topic: 'Überschrift'
      text: 'Nachricht'
      author: 'Absender'
      password: 'Passwort'
      type: 'Suche/Biete'
      image: 'Bild'
